import MobileMenuIcon from "./MobileMenuIcon";

const Header = () => {
  return (
    <div className="header">
      <MobileMenuIcon />
      <h1>Logo here</h1>
    </div>
  );
};

export default Header;
